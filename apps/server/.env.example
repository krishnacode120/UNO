PORT=4000
CLIENT_ORIGIN=http://localhost:5173
MONGODB_URI=
# Set to the exact number of trusted reverse proxies (1 on the Render template).
# Leave at 0 when exposing the Node server directly.
TRUST_PROXY_HOPS=0
